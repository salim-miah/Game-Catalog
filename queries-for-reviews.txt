create table reviews
(
	review_id int,
	list_id int,
	entry_id varchar(19),
	review varchar(5000),
	primary key(review_id,list_id,entry_id),
	foreign key(list_id,entry_id) references game_list(list_id,entry_id)
);
